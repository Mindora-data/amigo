# NIÑO OpenAPI

The live OpenAPI document is served by the local API:

```text
GET /openapi.json
```

The document is generated from the same endpoint catalog used by `GET /`, so the
root endpoint list and OpenAPI paths stay aligned by tests.
